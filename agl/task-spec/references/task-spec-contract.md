# Task Spec Contract

An agentLIBRE task spec must define the problem, goal, scope, non-goals,
implementation steps, acceptance criteria, and verification commands.

Specs are planning artifacts. They must not claim implementation status until
the corresponding code has landed and passed the relevant checks.
